const mongoose = require('mongoose')
const Schema = mongoose.Schema

const <%= props.name %>Schema = new Schema({

}, {timestamps: true})

module.exports = mongoose.model('<%= `${props.name.charAt(0).toUpperCase()}${props.name.slice(1)}` %>', userSchema)