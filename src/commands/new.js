module.exports = {
  name: 'new:template',
  description: 'This command not workink',
  run: async toolbox => {
    const { print } = toolbox

    print.info('Generate new project from template')
  }
}
